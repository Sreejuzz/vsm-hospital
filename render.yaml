services:
  - type: web
    name: vsm-hospital
    runtime: node
    buildCommand: echo "No build step"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
